# Baked in at build time - see SapSessionManager#loadFakeModeFlag() and
# SAP-Enhancement/SAP-Fake-Mode.md. Only "mvn ... -Dsap.fakeMode=true" sets this to true;
# a plain "mvn clean install" always ships false.
sap.fakeMode=${sap.fakeMode}
