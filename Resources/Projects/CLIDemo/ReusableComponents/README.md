# CLIDemo / ReusableComponents

This folder is intentionally available for the CLI to manipulate (e.g.
extracting steps into reusable components from the new commands once
they ship). Leave the empty subfolders in place.
